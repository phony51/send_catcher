services:
  send-catcher:
    build: .
    restart: unless-stopped
    volumes:
      - ./sessions:/app/sessions
      - ./configuration.json:/app/configuration.json:ro
      - ./logs:/app/logs
    user: "1000:1000"
    tty: true
    stdin_open: true